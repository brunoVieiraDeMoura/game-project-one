# Matriz de auditoria — Monstros

Fonte da verdade rAthena: `MobDatabase::parseBodyNode` (`rathena/src/map/mob.cpp:4977-5535`),
`MobDatabase::parseDropNode` (`mob.cpp:4844-4929`), `MobDatabase::loadingFinished` (`mob.cpp:5537-5596`),
`db/re/mob_db.yml:18-79` (defaults), `doc/mob_db.txt`, `doc/mob_db_mode_list.txt`,
`rathena/sql-files/mob_db_re.sql:6-177`. Schema: `packages/game-data/src/monster.ts`.
UI: `apps/admin/components/MonsterForm.tsx`.

| Campo | UI hoje | Zod | rAthena — tipo | rAthena — faixa/enum | Fonte | Obrigatório? | Sentinelas | UI proposta | Restrição proposta | Divergência | Risco de compat | Fase | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | Input(number), disabled edit | `int().positive()` | uint32 | 1000-3999 ou 20020-31999 (exclusivo) | mob.cpp:4980-4996, MIN/MAX_MOB_DB mob.cpp:61-64 | sim | — | sem mudança (disabled) | — | **A7**: zod só exige `positive()`, rAthena rejeita fora das duas faixas | baixo (criar com id fora da faixa é rejeitado pelo loader — silencioso) | fora do escopo aprovado (não implementar) | reportado |
| `aegisName` / `name` | Input(text) × 2 | `string().min(1)` × 2 | string | ≤24 chars (`NAME_LENGTH`) | mob.cpp:5000-5028; mmo.hpp:154 | sim (novo) | excede → resize+warn | sem mudança | maxLength=24 (opcional) | nenhuma | baixo | fora de escopo | pendente |
| `title` | Input(text), `""`→undefined | `string().optional()` | string | ≤24 chars | mob.cpp:5433 | não | — | sem mudança | — | nenhuma | baixo | fora de escopo | pendente |
| `level` | Input(number) | `int().nonnegative()` | uint16 | 0..65535, **sem checagem no parse** | mob.cpp:5044-5053 | não | default 1 | NumberField min=0 max=65535 | min=0 max=65535 step=1 int | nenhuma | nenhum | Fase1 | feito |
| `hp` | Input(number) | `int().nonnegative()` | uint32 | `cap_value(...,1,UINT32_MAX)` no loadingFinished — **mínimo real é 1, não 0** | mob.cpp:5053-5062; mob.cpp:5589-5590 | sim | — | NumberField min=1 max=4294967295 | min=1 max=4294967295 step=1 int | **A7**: zod aceita 0 (`nonnegative`), rAthena grampeia pra 1 no load | baixo (0 vira 1 silenciosamente, sem perda real) | Fase1 | feito |
| `sp` | Input(number) | `int().nonnegative().default(0)` | uint32 | coluna `mediumint unsigned` = 0..16777215 (mais estreita que uint32 lógico) | mob.cpp:5062; mob_db_re.sql | não | — | NumberField min=0 max=16777215 | min=0 max=16777215 step=1 int | **A7**: teto real de gravação é a coluna SQL | médio | Fase1 | feito |
| `baseExp`/`jobExp`/`mvpExp` | Input(number) × 3 | `int().nonnegative()` (base/job req.), mvp default 0 | uint64 (`t_exp`) | `cap_value(exp*rate/100, 0, MAX_EXP)`; MAX_EXP = INT64_MAX (PACKETVER≥20170830) senão INT32_MAX; coluna SQL `int(11) unsigned` = 0..4294967295 (BEM mais estreita que uint64) | mob.cpp:5071-5096; const.hpp:63-68; mob_db_re.sql | sim(base/job)/não(mvp) | — | NumberField min=0 max=4294967295 | min=0 max=4294967295 step=1 int | **A7**: coluna SQL é o teto real de gravação (4.29B), não o uint64 lógico | médio (exp de evento >4.29B truncaria no INSERT) | Fase1 | feito |
| `stats.{str,agi,vit,int,dex,luk}` | Input(number) × 6 | `int()` (BaseStatsSchema, sem min/max) | uint16, `max(0,stat)` no store | coluna `smallint unsigned` 0..65535 | mob.cpp:5166-5218; mob_db_re.sql | sim | default 1 | NumberField min=0 max=65535 | min=0 max=65535 step=1 int | **A7**: zod não tem NENHUM limite hoje (`BaseStatsSchema` é `z.number().int()` cru) | baixo | Fase1 | feito |
| `attack`/`magicAttack` | Input(number) × 2 | `attack` required nonneg; `magicAttack` default 0 | uint16 | 0..65535; RE: `Attack2`→`rhw.matk` | mob.cpp:5098-5120 | sim/não | — | NumberField min=0 max=65535 | min=0 max=65535 step=1 int | nenhuma | nenhum | Fase1 | feito |
| `defense`/`magicDefense` | Input(number) × 2 | `int().default(0)` × 2 | uint16 → `defType` | RE int16 -32768..32767 (`DEFTYPE_MIN/MAX`) | mob.cpp:5120-5148; const.hpp:48-56 | não | >DEFTYPE_MAX → cap+warn | NumberField min=-32768 max=32767 | min=-32768 max=32767 step=1 int | nenhuma | nenhum | Fase1 | feito |
| `resistance`/`magicResistance` | Input(number) × 2 | `int().default(0)` × 2 | uint16 (RENEWAL-only no reader) | 0..65535 | mob.cpp:5148-5166; mob.cpp:5791-5796 | não | — | NumberField min=0 max=65535 | min=0 max=65535 step=1 int | nenhuma | nenhum | Fase1 | feito |
| `attackRange`/`skillRange`/`chaseRange` | Input(number) × 3 | `attackRange positive().default(1)`, outros `nonnegative().default(0)` | uint16 | coluna `tinyint(4) unsigned` = **0..255** (mais estreita) | mob.cpp:5220-5245; mob_db_re.sql | não | AttackRange 1-2=melee, ≥3=ranged; ChaseRange≥1000=persegue mapa inteiro (**mas coluna só vai a 255!**) | NumberField min=0 max=255 | min=0 max=255 step=1 int | **A7**: doc diz "ChaseRange≥1000 = persegue por todo o mapa" mas a coluna SQL real (`tinyint unsigned`) NUNCA alcança 1000 — sentinela documentado é inatingível via este banco | **alto** — funcionalidade documentada (perseguição de mapa inteiro) pode ser impossível de configurar pelo schema SQL atual | Fase1 (respeitar teto real da coluna=255, reportar o gap) | pendente |
| `size` | Select (3) | `SizeSchema` enum | `"Size_"+s` | `Small Medium Large` (3) | mob.cpp:5247-5269; mob.hpp:114-120 | sim | inválido → `Small` | sem mudança | — | nenhuma | nenhum | — | feito |
| `race` | Select (12) | `RaceSchema` enum | `"RC_"+s` | `Formless Undead Brute Plant Insect Fish Demon DemiHuman Angel Dragon Player_Human Player_Doram` (12) | mob.cpp:5269-5291; map.hpp:324-340 | sim | inválido → `Formless` | sem mudança | — | nenhuma | nenhum | — | feito |
| `element.type` | Select (10) | `ElementSchema` (10) | `"ELE_"+s` | `Neutral Water Earth Fire Wind Poison Holy Dark Ghost Undead` (10, `Dark`↔`shadow` já traduzido em `mysql-monster-row.ts`) | mob.cpp:5322-5344; map.hpp:390-407 | sim | inválido → `Neutral` | sem mudança (já traduzido) | — | nenhuma | nenhum | — | feito |
| `element.level` | Input(number) **já com min/max** | `int().min(1).max(4)` | uint16 | 1..4 (`MAX_ELE_LEVEL`) | mob.cpp:5344-5358; map.hpp:422 | sim | — | sem mudança (já correto) | min=1 max=4 | nenhuma | nenhum | — | feito |
| `walkSpeed` | Input(number) | `int().positive().default(200)` | uint16 | `MIN_WALK_SPEED(20)..MAX_WALK_SPEED(1000)` | mob.cpp:5358-5372; mmo.hpp:93-96 | não | — | NumberField min=20 max=1000 | min=20 max=1000 step=1 int | nenhuma | nenhum | Fase1 | feito |
| `attackDelayMs` | Input(number) | `int().nonnegative().default(0)` | uint16 | `cap_value(speed, MAX_ASPD_NOPC(100), MIN_ASPD(8000))` — **note: nomes trocados no rAthena, o "min" real é 100 e "max" é 8000** | mob.cpp:5372-5381; status.hpp:44,55 | não | — | NumberField min=100 max=8000 | min=100 max=8000 step=1 int | **A7**: zod só tem `nonnegative()`, sem teto | baixo | Fase1 | feito |
| `attackMotionMs` | Input(number) | `int().nonnegative().default(0)` | uint16 | `cap_value(speed, 1, 8000)` | mob.cpp:5381-5391 | não | `adelay = max(adelay, amotion)` no loadingFinished | NumberField min=1 max=8000 | min=1 max=8000 step=1 int | **A7** | baixo | Fase1 | feito |
| `clientAttackMotionMs` | **sem UI** | `int().nonnegative().optional()` | uint16 | `cap_value(1, 65535)` | mob.cpp:5391-5403 | não | ausente ⇒ herda `attackMotionMs` | fora do escopo aprovado (Fase1 não adiciona campo novo à UI, só valida os existentes) | — | campo sem controle nenhum — achado, não corrigir aqui | baixo | fora de escopo | reportado |
| `damageMotionMs` | Input(number) | `int().nonnegative().default(0)` | uint16 | escalado por `monster_damage_delay_rate` | mob.cpp:5403-5415 | não | — | NumberField min=0 max=65535 | min=0 max=65535 step=1 int | nenhuma | nenhum | Fase1 | feito |
| `damageTaken` | Input(number) | `int().default(100)` | uint16, `asUInt16Rate(...,100)` | **1..100 obrigatório** (0 rejeitado) | mob.cpp:5415-5424 | não | fora da faixa → `return 0` (falha o load inteiro do mob!) | NumberField min=1 max=100 | min=1 max=100 step=1 int | **A7**: zod não tem NENHUM limite | **alto** — valor fora de 1..100 faz o mob inteiro falhar o load no rAthena | Fase1 | feito |
| `groupId` | Input(number) | `int().default(0)` | uint16, `asUInt16Rate(...)` default max 10000 | **1..10000**; 0 não pode ser gravado explicitamente (só surge do default do construtor) | mob.cpp:5424-5433 | não | 0 = nenhum grupo (só via ausência) | NumberField min=0 max=10000, hint "0 = nenhum grupo (não grave 0 manualmente — deixe vazio)" | min=0 max=10000 step=1 int | **A7** | médio — usuário pode digitar 0 achando que "desliga" o grupo, e o writer real rejeitaria | Fase1 | feito |
| `ai` | Input(text) livre | `string().default("06")` | `"MONSTER_TYPE_"+s` | `01 02 03 04 05 06 07 08 09 10 11 12 13 17 19 20 21 24 25 26 27 ABR_PASSIVE ABR_OFFENSIVE` (25 valores) | mob.cpp:5446-5468; mob.hpp:151-182 | não | inválido → `06` (passivo) | **Select** (Fase1, campo aprovado) | enum fechado (25) + escape p/ legado | **A5**: `aiMode`/`chasesAttacker` são derivações de `ai`/`modes` e nada mantém as 3 consistentes | alto hoje (texto livre aceita typo, writer usa default calado) | **Fase1** | feito |
| `aiMode` | Select (5) — DERIVADO de `ai`+`modes`, não campo primário do rAthena | `MonsterAiModeSchema.default("passive")` | não existe no rAthena — é conveniência do projeto | `passive aggressive assist looter plant` | monster.ts:101-107 (comentário do projeto) | não | — | **NÃO tocar nesta rodada** (ver A5) | — | é campo DO PROJETO, sem correspondente 1:1 no rAthena | ver A5 | fora de escopo (A5) | registrado |
| `class` | Select (5) | `MonsterClassSchema` | `"CLASS_"+s` | `Normal Boss Guardian Battlefield Event` (5) | mob.cpp:5468-5490; mob.hpp:185-194 | não | inválido → `Normal`; classe injeta modos extras no loadingFinished (Boss⇒detector+statusimmune+knockbackimmune, etc.) | sem mudança | — | nenhuma | nenhum | — | feito |
| `chasesAttacker` | Checkbox — DERIVADO de `modes` | `bool().default(false)` | não existe isolado — é `changechase\|changetargetchase` | — | monster.ts:107 (`changechase \|\| changetargetchase`) | não | — | **NÃO tocar nesta rodada** (ver A5) | — | ver A5 | ver A5 | fora de escopo (A5) | registrado |
| `modes[]` | Input(text) CSV livre | `array(string()).default([])` | map bool `"MD_"+s`, OR/AND-NOT em `status.mode` | 26 flags: `canmove looter aggressive assist castsensoridle norandomwalk nocast canattack castsensorchase changechase angry changetargetmelee changetargetchase targetweak randomtarget ignoremelee ignoremagic ignoreranged mvp ignoremisc knockbackimmune teleportblock fixeditemdrop detector statusimmune skillimmune` | mob.cpp:5490-5521; mmo.hpp:242-272 | não | inválido → skip+warn (não falha o mob inteiro) | Fase2 (MultiSelectField) — **fora do escopo desta rodada** | — | nenhuma (26 confirmados == `MODE_COLUMNS` em `mysql-monster-row.ts`) | alto hoje (CSV livre, typo é silenciosamente descartado) | Fase2a | feito |
| `raceGroups[]` | Input(text) CSV livre | `array(string()).default([])` | map bool `"RC2_"+s` | 42 grupos (`Goblin Kobold Orc ... Encroached_Gephenia`); `Guardian`/`Battlefield` DEPRECIADOS sem constante de script exportada apesar da coluna SQL existir | mob.cpp:5291-5322; map.hpp:342-387; script_constants.hpp:3514-3553 | não | inválido → skip+warn | Fase2 (MultiSelectField) — **fora do escopo desta rodada** | — | nenhuma | alto hoje | Fase2a | feito |
| `mvp` | Checkbox — DERIVADO de `modes.includes("mvp")` no read; **NÃO escrito diretamente** no write (`mysql-monster-row.ts:249`, só via `modes` chega a `mode_mvp`) | `bool().default(false)` | é o bit `MD_MVP` de `modes` | — | mob.cpp mode MVP; mysql-monster-row.ts:202,249 | não | — | sem mudança na UI, mas **achado**: alternar só `mvp` sem alternar `modes` não persiste | — | write-path: campo `mvp` isolado não é gravado, só via `modes` | **alto** — usuário marca "MVP" no checkbox, salva, e se `modes` não tiver `mvp` o toggle não bate no banco | fora de escopo (backend), reportar | reportado |
| `drops[].itemId` / `mvpDrops[].itemId` | Input(number) | `int().positive()` | string (AegisName, resolvido via lookup) | item precisa existir; senão → skip+warn | mob.cpp:4844-4929 (`parseDropNode`) | sim (por linha) | — | Fase3 (picker de item) — **fora do escopo desta rodada** | — | nenhuma | médio (id inexistente é silenciosamente descartado, sem feedback) | Fase3 | não fazer nesta rodada |
| `drops[].rate` / `mvpDrops[].rate` | Input(number) **já com step=0.01** | `number().min(0).max(100)` (percent) | `asUInt16Rate` (n/10000) | **1..10000** (0 REJEITADO — `"needs to be at least 1"`) | mob.cpp:4844-4929 (Rate); database.cpp:315-331 | sim (por linha) | 10000=100% | NumberField min=0.01 max=100 step=0.01, hint "0 é rejeitado pelo servidor — mínimo real 0,01%" | min=0.01 max=100 step=0.01 | **A7**: zod aceita 0 (`min(0)`), rAthena rejeita | **alto** — usuário grava rate=0 achando "sem chance", servidor descarta a linha inteira no load | Fase1 | feito |
| `drops[].stealProtected` | Checkbox | `bool().default(false)` | bool | só é lido/significativo pra `Drops` (MVP sempre true na prática) | mob.cpp (`StealProtected`) | não | — | sem mudança | — | nenhuma | nenhum | — | feito |
| `drops[].randomOptionGroup` | Input(text) livre | `string().optional()` | string, nome de grupo | inválido → warn, "sem grupo" | mob.cpp (`RandomOptionGroup`) | não | — | Fase3 (picker de grupo) — fora do escopo | — | nenhuma | médio | Fase3 | não fazer nesta rodada |
| `drops[].index` | **sem UI** | `int().optional()` | uint16 | `>= max` → warn+skip; `> size` → skip; `==size` → append; `<size` → OVERWRITE | mob.cpp:4844-4929 | não | posicional — sobrescreve slot existente | fora do escopo (Fase1 não adiciona campo novo) | — | campo sem controle nenhum — achado | médio | fora de escopo | reportado |
| `fleeOverride`/`hitOverride` | Input(number), `""`→undefined | `int().optional()` × 2 | **não existem no mob_db do rAthena — sem coluna, sem leitura/escrita** | n/a | grep `mysql-monster-row.ts` (zero refs) | não | — | sem mudança na UI (campo já opcional) | — | campo do PROJETO sem correspondente rAthena | write-path: nunca persistido no MariaDB | fora de escopo (backend) | reportado |
| `skills[]` (MonsterSkillUseSchema) | **sem UI nenhuma** | `array(MonsterSkillUseSchema).default([])` | CSV `mob_skill_db.txt`, 19 colunas fixas | ver `monster-skills.md` | mob.cpp:6372-6623 | não | — | Fase4 (bloqueada por A4) | — | **A4**: zero persistência SQL | alto (perda de dado se editado) | Fase4, bloqueada | não fazer (aprovado como fora de escopo) |
| `spawns[]` | `spawns[].mapId` Input(text) livre; `.amount/.respawnTimeMs/.respawnVarianceMs` Input(number); `.area` 1 input parseado por vírgula; `.boss` Checkbox | `MonsterSpawnSchema` | linhas de script NPC (`npc/re/mob/*.txt`), NÃO é banco | n/a — spawn não é campo de `mob_db` | mysql-monster-row.ts:17-20 (comentário) | não | — | Fase3 (`mapId` → picker de mapa, `listMaps` já existe) — fora do escopo desta rodada; `.amount/.respawnTimeMs/.respawnVarianceMs` são candidatos a NumberField simples (sem faixa fechada do rAthena, é config nossa de script) | `.amount` min=1; `.respawnTimeMs`/`.respawnVarianceMs` min=0 | nenhuma | baixo (não é DB do rAthena, é geração de script) | mapId→Fase3; numéricos→Fase1 opcional (não crítico, sem sentinela documentada) | pendente (numéricos), não fazer (mapId) |

## Notas
- `ai`/`aiMode`/`chasesAttacker`/`mvp` são 4 campos com relação de derivação nunca resolvida — ver **A5** no risk-report. Esta rodada só toca `ai` (Select), sem tocar a lógica de derivação.
- Linha `skills[]` confirma **A4** (zero persistência SQL) — Fase 4 não entra nesta rodada por decisão explícita do usuário.
