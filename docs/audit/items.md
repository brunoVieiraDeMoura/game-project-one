# Matriz de auditoria — Itens

Fonte da verdade rAthena: `ItemDatabase::parseBodyNode` (`rathena/src/map/itemdb.cpp:49-1115`),
`db/re/item_db_equip.yml:18-90` (defaults), `doc/item_db.txt`,
`rathena/sql-files/item_db_re.sql:6-120`. Schema: `packages/game-data/src/item.ts`.
UI: `apps/admin/components/ItemForm.tsx`, `apps/admin/components/EffectsEditor.tsx`.

| Campo | UI hoje | Zod | rAthena — tipo | rAthena — faixa/enum | Fonte | Obrigatório? | Sentinelas | UI proposta | Restrição proposta | Divergência | Risco de compat | Fase | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | Input(number), disabled em edit | `int().positive()` | `t_itemid` (uint32) | 0..4294967295 (`id int(10) unsigned`) | itemdb.cpp:52; item_db_re.sql:7 | sim | — | sem mudança (já disabled) | — | nenhuma | nenhum | — | feito |
| `aegisName` | Input(text) | `string().min(1)` | string | ≤50 chars, único; excede → cap+warn | itemdb.cpp:67-80; ITEM_NAME_LENGTH=50 mmo.hpp:161 | sim (novo) | — | sem mudança (texto livre é correto — nome é string, não enum) | maxLength=50 (soft) | nenhuma | baixo (sem `maxLength` hoje, deixa digitar >50 e só o writer avisa) | Fase1 (opcional, fora do escopo aprovado — não implementar) | pendente |
| `name` | Input(text) | `string().min(1)` | string | ≤50 chars | itemdb.cpp:104-111 | sim (novo) | — | sem mudança | — | nenhuma | baixo | fora de escopo | pendente |
| `aliasName` | Input(text) | `string().optional()` | string → item lookup | item inexistente → warn + descarta | itemdb.cpp:608 | não | `""`/ausente = sem alias | Fase 3 (picker de item) | — | nenhuma | médio (typo vira "sem alias" silencioso no writer) | Fase3 | não fazer (fora de escopo) |
| `type` | Select (12 opts) | `ItemTypeSchema` enum | `"IT_"+s` | `Healing Usable Etc Armor Weapon Card PetEgg PetArmor Ammo DelayConsume ShadowGear Cash` (12) | itemdb.cpp:134-153; mmo.hpp:223-239 | não | inválido → `IT_ETC` | sem mudança (já é Select fechado, bate com o enum real) | — | nenhuma | nenhum | — | feito |
| `subType` | Select (`""` + 35 opts) | `ItemSubTypeSchema` | `"W_"`/`"AMMO_"`/`"CARD_"` conforme `type` | armas 24 + munição 9 + carta 2 = 35 | itemdb.cpp:154-191; pc.hpp:959-1016 | não | inválido em arma→`W_Fist`, munição→`AMMO_None`(mas item NÃO aceita `AMMO_NONE`), carta→`Card_Normal` | sem mudança | — | nenhuma | nenhum | — | feito |
| `buyPrice` | Input(number) | `int().nonnegative().default(0)` | uint32 | 0..MAX_ZENY(2147483647); coluna `mediumint unsigned` = 0..16777215 (MAIS ESTREITA) | itemdb.cpp:201-218; item_db_re.sql; mmo.hpp:82 | não | ausente + `Sell` presente ⇒ `Buy=Sell*2` | NumberField min=0 max=16777215 | min=0 max=16777215 step=1 int | **A7**: teto real de gravação é a coluna SQL (16.777.215), mais estreito que `MAX_ZENY` do C++ | médio — valor entre 16.777.216 e MAX_ZENY passa no zod/C++ mas trunca/erra no INSERT MySQL | Fase1 | feito |
| `sellPrice` | Input(number) | `int().nonnegative().default(0)` | uint32 | idem buyPrice + regra anti-exploit (`buy/124 < sell/75` → força sell=1) | itemdb.cpp:220-239 | não | ausente + `Buy` presente ⇒ `Sell=Buy/2` | NumberField min=0 max=16777215 | min=0 max=16777215 step=1 int | mesma de buyPrice (A7) | médio | Fase1 | feito |
| `weight` | Input(number) | `int().nonnegative().default(0)` | uint32 | coluna `smallint unsigned` = 0..65535 | itemdb.cpp:241; item_db_re.sql | não | 10 = 1.0 peso real | NumberField min=0 max=65535 | min=0 max=65535 step=1 int | nenhuma | nenhum | Fase1 | feito |
| `attack` | Input(number) | `int().default(0)` (assinado) | uint32 (não-negativo no C++!) | coluna `smallint unsigned` 0..65535; não-Weapon/Ammo com atk>0 → força 0 | itemdb.cpp:253; itemdb.cpp:1166 | não | — | NumberField min=0 max=65535 | min=0 max=65535 step=1 int | **A7**: zod aceita negativo (`int()` sem `.nonnegative()`), rAthena não | baixo (negativo nunca fez sentido, mas zod permite hoje) | Fase1 | feito |
| `magicAttack` | Input(number) | `int().default(0)` | uint32, só RENEWAL | 0..65535; não-Weapon → força 0 | itemdb.cpp:266; itemdb.cpp:1173 | não | — | NumberField min=0 max=65535 | min=0 max=65535 step=1 int | mesma de `attack` (A7) | baixo | Fase1 | feito |
| `defense` | Input(number) | `int().default(0)` | uint16 → `defType` | `DEFTYPE_MIN..MAX`: RE int16 (-32768..32767); coluna `smallint unsigned` 0..65535; não-Armor/ShadowGear → força 0 | itemdb.cpp:279-296; const.hpp:48-56 | não | >DEFTYPE_MAX → cap+warn | NumberField min=0 max=32767 | min=0 max=32767 step=1 int | nenhuma (zod já assinado, mas rAthena nunca grava negativo aqui) | nenhum | Fase1 | feito |
| `range` | Input(number) | `int().nonnegative().default(0)` | uint16 | 0..AREA_SIZE (`battle_config.area_size`, runtime — usar 0..255 como teto de UI, coluna `tinyint unsigned`); não-Weapon → força 0 | itemdb.cpp:296-313; item_db_re.sql (`range tinyint(2) unsigned`) | não | — | NumberField min=0 max=255 | min=0 max=255 step=1 int | nenhuma | nenhum | Fase1 | feito |
| `slots` | Input(number) **já com min/max** | `int().min(0).max(4).default(0)` | uint16 | 0..MAX_SLOTS(4) | itemdb.cpp:313-330; mmo.hpp:80 | não | >4 → cap+warn | sem mudança (já correto) | min=0 max=4 step=1 | nenhuma | nenhum | — | feito |
| `jobs[]` | Checkbox × 27 | `array(string()).default(["all"])` | map bool `"EAJ_"+s` | ~35 nomes 1-1/2-1/2-2 + `All` | itemdb.cpp:330-376; script_constants.hpp:276-380 | não | nome inválido → warn, `break` (para no meio!) | sem mudança (já checkbox fechado) | — | **A3** (write-path): nome sem coluna em `JOB_COLUMNS` (apps/api) é descartado no save | write-path lossy já documentado em A3 | fora de escopo (backend) | reportado |
| `classes[]` | Checkbox × 10 | `array(CharacterVariantSchema).default([])` | map bool `"ITEMJ_"+s` + especial `All` | `Normal Upper Baby Third Third_Upper Third_Baby Fourth All_Upper All_Baby All_Third` (10) | itemdb.cpp:376-423; itemdb.hpp:211-232 | não | vazio ⇒ `class_all=1` no SQL (ver A3) | sem mudança | — | **A3**: `all_upper/all_baby/all_third` sem coluna em `CLASS_COLUMNS` (apps/api), descartados no write | write-path lossy | fora de escopo (backend) | reportado |
| `gender` | Select (hardcoded both/male/female) | `GenderSchema.default("both")` | `"SEX_"+s` | `Female Male Both` (3) | itemdb.cpp:423-446; mmo.hpp:1115-1120 | não | `defaultGender()` sobrepõe p/ certos ids/armas | sem mudança | — | nenhuma | nenhum | — | feito |
| `locations[]` | Checkbox × 23 | `array(EquipLocationSchema).default([])` | map bool `"EQP_"+s` | 23 no schema; rAthena real tem os mesmos 21 bits + aliases `Both_Hand/Both_Accessory/...` | itemdb.cpp:446-487; mmo.hpp:336-363 | não (só se equip) | nome inválido → força `type=IT_ETC` (!) | sem mudança na UI | — | **A3**: `both_hands`/`both_accessories` sem coluna SQL (apps/api), descartados no write | write-path lossy, alto (edita local `armas ambas mãos` e o save apaga silenciosamente) | fora de escopo (backend) | reportado |
| `weaponLevel` | Input(number), `""`→undefined | `int().min(0).max(5).optional()` | uint16 | 0..MAX_WEAPON_LEVEL (RE=5, Pre-RE=4); não-weapon → 0; weapon sem valor → 1 | itemdb.cpp:487-509; mmo.hpp:132-140 | não | 0 = "não setado", promovido a 1 se weapon | NumberField min=0 max=5 | min=0 max=5 step=1 int | **A7**: zod fixo em 5 (RE); se servidor rodar Pre-RE o teto real é 4 — projeto já confirmou ruleset=renewal (CLAUDE.md), então 5 está correto | nenhum (ruleset fixo) | Fase1 | feito |
| `armorLevel` | Input(number), `""`→undefined | `int().min(0).max(2).optional()` | uint16 | 0..MAX_ARMOR_LEVEL (RE=2, Pre-RE=1) | itemdb.cpp:509-533; mmo.hpp:132-140 | não | 0 = não setado, promovido a 1 se armor | NumberField min=0 max=2 | min=0 max=2 step=1 int | nenhuma (RE confirmado) | nenhum | Fase1 | feito |
| `equipLevelMin` | Input(number) | `int().nonnegative().default(0)` | uint16 | 0..MAX_LEVEL(275) | itemdb.cpp:533-550; map.hpp:78 | não | — | NumberField min=0 max=275 | min=0 max=275 step=1 int | nenhuma | nenhum | Fase1 | feito |
| `equipLevelMax` | Input(number) | `int().nonnegative().default(0)` | uint16 | 0..MAX_LEVEL(275) | itemdb.cpp:550-572 | não | **default real é MAX_LEVEL(275), não 0** (doc do yml está errado: item_db_equip.yml:44 diz "Default: 0") | NumberField min=0 max=275, hint "vazio = 275 (sem teto)" | min=0 max=275 step=1 int | **A6** doc-vs-código (default errado no doc oficial) | médio — zod default é 0, rAthena real default é 275; um item sem este campo hoje grava/lê 0 (zod), quando deveria significar "sem teto" | Fase1 (hint apenas, sem mudar default do schema — A7) | feito |
| `refineable` | Checkbox | `bool().default(false)` | bool, **armazenado invertido** (`flag.no_refine = !refine`) | — | itemdb.cpp:572-584 | não | omitido ⇒ `no_refine=true` ⇒ NÃO refinável (mesmo sentido do zod default false) | sem mudança | — | nenhuma (defaults batem) | nenhum | — | feito |
| `gradable` | Checkbox | `bool().default(false)` | bool | — | itemdb.cpp:584-596 | não | — | sem mudança | — | nenhuma | nenhum | — | feito |
| `viewSprite` | Input(number) | `int().nonnegative().default(0)` | uint32 | coluna `smallint unsigned` 0..65535 | itemdb.cpp:596-608; item_db_re.sql (`view`) | não | shield com view=0 → força 1 | NumberField min=0 max=65535 | min=0 max=65535 step=1 int | nenhuma | nenhum | Fase1 | feito |
| `flags.dropEffect` | Input(text) livre | `string().optional()` | `"DROPEFFECT_"+s` | `None Client White_Pillar Blue_Pillar Yellow_Pillar Purple_Pillar Orange_Pillar Green_Pillar Red_Pillar` (9, PACKETVER-dependente) | itemdb.hpp:3263-3291; itemdb.cpp:627-747 | não | inválido → `DROPEFFECT_NONE` | **Select** (Fase 1, campo aprovado) | enum fechado (9) + escape p/ legado | nenhuma | alto hoje (texto livre aceita typo, writer descarta calado) | **Fase1** | feito |
| `flags.buyingStore` `.deadBranch` `.container` `.uniqueId` `.bindOnEquip` `.dropAnnounce` `.noConsume` | Checkbox × 7 | `bool().default(false)` × 7 | bool, chaves literais | — | itemdb.cpp:630-747 | não | `BuyingStore`/`UniqueId` em item não-empilhável → força false | sem mudança | — | nenhuma | nenhum | — | feito |
| `delay.durationMs` | Input(number) | `int().nonnegative()` (required se `delay` presente) | uint32 (segundos!) | — | itemdb.cpp:765-777 | condicional | **unidade: schema é ms, rAthena é SEGUNDOS** | NumberField min=0 | min=0 step=1 int, hint "ms — writer converte p/ segundos" | **A7**: confirmar no writer que a conversão ms→s existe (achado, não corrigir aqui) | alto se writer não converter — dado 1000× errado | Fase1 (limite) + reportar conversão em A7 | feito |
| `delay.statusId` | Input(text) livre | `string().optional()` | `"SC_"+s` | catálogo de status (1028 `SC_*`) | itemdb.cpp:777-806 | não | inválido → `SC_NONE` | Fase 3 (picker de status) | — | nenhuma | médio (typo vira "sem status" silencioso) | Fase3 | não fazer (fora de escopo) |
| `stack.amount` | Input(number) | `int().positive()` (required se `stack` presente) | uint16 | coluna `smallint unsigned` 0..65535; não-stackable → força 0 | itemdb.cpp:806-823 | condicional | — | NumberField min=1 max=65535 | min=1 max=65535 step=1 int | nenhuma | nenhum | Fase1 | feito |
| `stack.inventory/.cart/.storage/.guildStorage` | Checkbox × 4 | bool × 4, `inventory` default **true** | bool | — | itemdb.cpp:823-869 | não | **A6**: yml doc diz `Inventory (Default: true)` mas código realmente aplica `false` como default (`itemdb.cpp:831-832`) | sem mudança | — | **A6** doc-vs-código, zod já segue o comportamento do CÓDIGO (`inventory: bool().default(true)` — CONFERIR, zod pode estar seguindo o DOC errado) | **alto** — zod tem `.default(true)` (item.ts:57); rAthena de fato usa `false`; requer checagem extra (A7) | reportar (A6+A7), não corrigir schema | reportado |
| `noUse.overrideGroupLevel` | Input(number) | `int().default(100)` | uint16 | 0..100 (cap) | itemdb.cpp:883-900 | não | 100 = ninguém passa (grupo máximo) | NumberField min=0 max=100 | min=0 max=100 step=1 int | nenhuma | nenhum | Fase1 | feito |
| `noUse.sitting` | Checkbox | `bool().default(false)` | bool | — | itemdb.cpp:900-921 | não | — | sem mudança | — | nenhuma | nenhum | — | feito |
| `trade.overrideGroupLevel` | Input(number) | `int().default(100)` | uint16 | 0..100 (cap) | itemdb.cpp:921-938 | não | — | NumberField min=0 max=100 | min=0 max=100 step=1 int | nenhuma | nenhum | Fase1 | feito |
| `trade.noDrop/.noTrade/.tradePartnerOnly/.noSell/.noCart/.noStorage/.noGuildStorage/.noMail/.noAuction` | Checkbox × 9 | bool × 9 default false | bool | — | itemdb.cpp:938-1050 | não | — | sem mudança | — | nenhuma | nenhum | — | feito |
| `onUse`/`onEquip`/`onUnequip` (EffectList) | `EffectsEditor` (editável) | `EffectListSchema.optional()` | `parse_script` (Script/EquipScript/UnEquipScript) | script rAthena livre | itemdb.cpp:~1050-1110 | não | — | sem mudança na UI (fora do escopo desta rodada — é conteúdo de script, não campo escalar) | — | **A3-adjacente**: `onUse/onEquip/onUnequip` NUNCA são lidos/escritos no MariaDB (`mysql-item-row.ts` preserva `rawScript*` verbatim) — editar aqui é NO-OP contra o banco real | **alto**, já documentado no código-fonte (comentário do módulo) mas vale registrar formalmente | fora de escopo (arquitetural) | reportado (ver risk-report) |
| `EffectsEditor: .statusId` (grantStatus) | Input(text) livre, TODO no código | `string()` | `"SC_"+s` | catálogo status | status.cpp validateStatus | condicional | inválido no write real → erro de parse do script (não passa pelo loader de item, é `parse_script`) | Fase 3 (picker de status) | — | nenhuma | médio | Fase3 | não fazer (fora de escopo) |
| `EffectsEditor: .min/.max/.value/.durationMs/.chance/.skillId/.level` | Input(number) | vários `number()`/`int()` | conteúdo de SCRIPT rAthena (não passa por parseBodyNode — é texto livre interpretado em runtime) | sem faixa fixa do loader (é script) | n/a — é script, não campo de DB | não | `chance` já limitado 0..100 no zod | sem mudança (não é campo de DB fechado, é parâmetro de script) | — | nenhuma | nenhum | fora de escopo | n/a |

## Notas
- `EffectsEditor` edita `onUse/onEquip/onUnequip`, mas via MariaDB (backend ativo, `hasRoDatabase()`) essas edições **não persistem** — achado grave, ver risk-report A-onUse.
- Linhas marcadas "Fase1" e "pendente" são o escopo real desta rodada (Tarefa #17/#18/#19). As demais (Fase3/backend/fora de escopo) só são registradas aqui, não implementadas.
