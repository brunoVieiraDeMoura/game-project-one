# Matriz de auditoria — Skills

Fonte da verdade rAthena: `SkillDatabase::parseBodyNode` (`rathena/src/map/skill.cpp:14778-15749`),
`db/re/skill_db.yml:18-140` (defaults), `doc/skill_db.txt`. Schema: `packages/game-data/src/skill.ts`.
UI: `apps/admin/components/SkillForm.tsx`. Write path (verdade dos enums fechados já usados por
`packages/game-data/src/rathena/skill-db-yaml.ts` / `skill-db-mapper.ts`).

| Campo | UI hoje | Zod | rAthena — tipo | rAthena — faixa/enum | Fonte | Obrigatório? | Sentinelas | UI proposta | Restrição proposta | Divergência | Risco de compat | Fase | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | Input(number), disabled edit | `int().positive()` | uint16 | qualquer uint16 (tabela `skilldb_id2idx[UINT16_MAX+1]`) | skill.cpp:14781; skill.hpp:328 | sim | — | sem mudança (disabled) | — | nenhuma | nenhum | — | feito |
| `aegisName` | Input(text) | `string().min(1)` | string (`Name`) | ≤40 chars (`SKILL_NAME_LENGTH`) | skill.cpp:14795; skill.hpp:46 | sim (novo) | excede → erro, `return 0` (falha o skill inteiro) | sem mudança | maxLength=40 (opcional) | nenhuma | médio (>40 chars derruba o skill inteiro no load) | fora de escopo | pendente |
| `name` | Input(text) | `string().min(1)` | string (`Description`) | ≤40 chars (`SKILL_DESC_LENGTH`) | skill.cpp:14810; skill.hpp:47 | sim (novo) | excede → erro, `return 0` | sem mudança | maxLength=40 (opcional) | nenhuma | médio | fora de escopo | pendente |
| `maxLevel` | Input(number) | `int().positive()` (SEM teto no zod) | uint16 | **1..13** (`MAX_SKILL_LEVEL`) | skill.cpp:14825-14839; skill.hpp:43 | sim (novo) | `==0 \|\| >13` → erro, `return 0` (falha o skill inteiro) | NumberField min=1 max=13 | min=1 max=13 step=1 int | **A7**: zod não tem teto (`positive()` só exclui ≤0) | **alto** — `maxLevel=14` passa no zod, falha no load do rAthena, skill some silenciosamente do servidor | Fase1 | feito |
| `type` | Select (5) | `SkillTypeSchema` | `"BF_"+s` | `None(0) Weapon Magic Misc` — só 4 valores válidos no C++ (`damageNature`, não `type`) | skill.cpp:14839-14865; battle.hpp:43-58 | não | — | ver nota abaixo — `type` (schema) mapeia p/ `TargetType`, `damageNature` mapeia p/ `Type` real | — | nomenclatura já resolvida no mapper (`skill-db-mapper.ts`); nada a mudar aqui | nenhum | — | feito |
| `damageNature` | Select (4) | `SkillDamageNatureSchema` | `"BF_"+s` (`Type` no YAML) | `none weapon magic misc` (4) — bate exato | skill.cpp:14839-14865 | não | inválido → `BF_NONE` | sem mudança | — | nenhuma | nenhum | — | feito |
| `hitType` | Select (4) | `SkillHitTypeSchema` | `"DMG_"+s` (`Hit`) | rAthena tem 15 valores (`Normal Pickup_Item Sit_Down Stand_Up Endure Splash Single Repeat Multi_Hit Multi_Hit_Endure Critical Lucy_Dodge Touch Multi_Hit_Critical Splash_Endure`), mas o WRITER só grava `Single`/`Multi_Hit` (`REV_HIT`) | skill.cpp:14942-14962; clif.hpp:691-707; skill-db-mapper.ts REV_HIT | não | `normal`/`critical` no schema geram WARN no writer (não são graváveis) | sem mudança na UI (já é enum fechado de 4, e os 4 já cobrem o que o writer aceita) | — | **A7**: zod schema (`normal single multi_hit critical`) inclui 2 valores que o writer não consegue emitir (`normal`, `critical`) | médio — usuário escolhe `normal`/`critical`, salva, writer emite warning e mantém valor antigo | fora de escopo (writer, não UI) | reportado |
| `target` | Select (6) | `SkillTargetSchema` | `"INF_"+s+"_SKILL"` (`TargetType`) | `Passive(0) Attack Ground Self Support Trap` (6) — bate exato | skill.cpp:14865-14882; skill.hpp:78-86 | sim | inválido → `return 0` | sem mudança | — | nenhuma | nenhum | — | feito |
| `interruptible` | Checkbox | `bool().default(true)` | bool (`CastCancel`) | — | skill.cpp:15153-15165 | não | default real também `true` | sem mudança | — | nenhuma | nenhum | — | feito |
| `castDefenseReduction` | Input(number), `""`→undefined | `int().optional()` | uint16 | 0..65535 | skill.cpp:15165-15177 | não | — | NumberField min=0 max=65535 | min=0 max=65535 step=1 int | nenhuma | nenhum | Fase1 | feito |
| `range` | `PerLevelField` (texto) | `perLevel(int())` **required** | `int32[13]` via `parseNode` | sem faixa fixa do loader (livre) | skill.cpp:14934-14942 | sim | `<5` = tratado como corpo-a-corpo (doc) | fora de escopo — `PerLevelField` não é `NumberField` (escalar-ou-array por nível), não faz parte da lista de 4 campos aprovados | — | nenhuma | baixo | fora de escopo | pendente |
| `damageFormula.element` | Input(text) dentro do bloco de fórmula | `SkillElementSchema.default("weapon")` | `"ELE_"+s` | 13: 10 elementos + `Weapon Endowed Random` (`ELE_NONE` explicitamente REJEITADO) | skill.cpp:14970-15028; map.hpp:390-407 | não | `ELE_NONE`/inválido → erro `return 0` (elemento é `perLevel`, este campo é a versão single-value do zod) | **Select** (Fase1, campo aprovado) | enum fechado (13) + escape p/ legado | nenhuma | alto hoje (texto livre) | **Fase1** | feito |
| `requirements.requiredState` | Input(text) livre | `string().optional()` | `"ST_"+s` | `None Hidden Riding Falcon Cart Shield Recover_Weight_Rate Move_Enable Water RidingDragon Wug RidingWug Mado ElementalSpirit ElementalSpirit2 Peco Sunstance Moonstance Starstance Universestance` (20) | skill.cpp:15445-15462; skill.hpp:697-718 | não | inválido → `return 0`; fonte já em `SkillDbStateSchema` (skill-db-yaml.ts) | **Select** (Fase1, campo aprovado) | enum fechado (20, de `SkillDbStateSchema.options`) + escape p/ legado | nenhuma | alto hoje | **Fase1** | feito |
| `requirements.hpCost/.apCost/.hpRateCost/.spRateCost/.apRateCost/.maxHpTrigger/.spiritSphereCost/.zenyCost/.ammoAmount` | `PerLevelField` (texto) × 9 | `perLevel(int()).optional()` × 9 | `parseNode(...,"Amount")` | sem faixa fixa (livre); `HpRateCost`/`SpRateCost`/`ApRateCost`: **positivo=% do atual, negativo=% do máximo** (sentinela de sinal) | skill.cpp:15290-15352 | não | `ammoAmount` sem `ammo`>0 → erro | fora de escopo — não é `NumberField` simples (per-level) | — | nenhuma | baixo | fora de escopo | pendente |
| `requirements.requiredWeapons[]` | CSV texto, cast `as typeof req.requiredWeapons` | `array(ItemSubTypeSchema).default([])` | `"W_"+s`, `All:true`⇒0 | 24 armas + especial `All` | skill.cpp:15354-15393; pc.hpp:959-994 | não | vazio ⇒ sem restrição (todas) | Fase2 (MultiSelectField) — fora do escopo desta rodada | — | nenhuma | alto hoje (CSV livre) | Fase2b | feito |
| `requirements.requiredAmmo[]` | CSV texto | `array(string()).default([])` | `"AMMO_"+s`, `None:true`⇒0 | 9 munições + especial `None` | skill.cpp:15393-15432; pc.hpp:998-1010 | não | — | Fase2 (MultiSelectField) — fora do escopo desta rodada | — | nenhuma | alto hoje | Fase2b | feito |
| `requirements.requiredEquipment[]` | CSV texto mapeado por `Number` | `array(int()).default([])` | map AegisName→bool | item precisa existir | skill.cpp:15543-15572 | não | inválido → `return 0` | Fase3 (picker de item) — fora do escopo desta rodada | — | nenhuma | médio | Fase3 | não fazer nesta rodada |
| `requirements.requiredStatuses[]` | Chip list + Select "+ catálogo" — **já é picker!** | `array(string()).default([])` | `"SC_"+s` bools | catálogo 1028 `SC_*` | skill.cpp:15462-15490 | não | inválido → `return 0` | sem mudança (já é o padrão-modelo do projeto) | — | nenhuma | nenhum | — | feito |
| `requirements.itemsConsumed[].itemId` | Input(number) livre | `int()` | string AegisName | item precisa existir | skill.cpp:15498-15543 | sim (por linha) | inválido → `return 0` | Fase3 (picker de item) — fora do escopo desta rodada | — | nenhuma | médio | Fase3 | não fazer nesta rodada |
| `requirements.itemsConsumed[].amount` | Input(number) | `int().nonnegative()` | `parseNode(...,"Amount")` | livre | skill.cpp:15498-15543 | sim | — | NumberField min=0 | min=0 step=1 int | nenhuma | nenhum | Fase1 | feito |
| `requirements.itemsConsumed[].level` | Input(number), `""`→undefined | `int().positive().optional()` | uint16 | 1..`maxLevel` do skill | skill.cpp:15498-15543 | não | fora da faixa → `return 0` (falha skill inteiro) | NumberField min=1 (max dinâmico = maxLevel do form, hint) | min=1, hint "≤ nível máximo da skill" | **A7**: teto real é dinâmico (depende de `maxLevel`), não representável em `field-limits.ts` estático | médio | Fase1 (só min=1, sem teto fixo — registrar limitação) | feito |
| `element` (perLevel) | `TokenListField` CSV | `perLevel(SkillElementSchema).default("weapon")` | igual `damageFormula.element` acima, mas por nível | 13 valores, por nível | skill.cpp:14970-15028 | não | mistura escalar-vs-array por nível | fora de escopo (não é o `damageFormula.element` aprovado — ver plano §Fase0, campo explicitamente excluído da conversão) | — | nenhuma | baixo | fora de escopo (Fase0 nota explícita) | pendente |
| `damageFlags[]` | `TokenListField` CSV | `array(string()).default([])` | map bool `"NK_"+s`, `bitset<NK_MAX>` | 11: `NoDamage Splash SplashSplit IgnoreAtkCard IgnoreElement IgnoreDefense IgnoreFlee IgnoreDefCard Critical IgnoreLongCard SimpleDefense` | skill.cpp:14882-14908; skill.hpp:62-75 | não | inválido → `return 0` (falha skill inteiro, DIFERENTE de `Flags` que só faz warn) | Fase2 (MultiSelectField) — fora do escopo desta rodada | — | fonte já em `SKILL_DB_DAMAGE_FLAGS` (skill-db-yaml.ts), lista exata | alto hoje (CSV livre, e um typo aqui derruba o skill inteiro no load, não só ignora a flag) | Fase2b | feito |
| `flags[]` | `TokenListField` CSV | `array(string()).default([])` | map bool `"INF2_"+s`, `bitset<INF2_MAX>` | 42 flags (`IsQuest`...`IgnoreNonCritAtkBonus`) | skill.cpp:14908-14934; skill.hpp:89-133 | não | inválido → warn+`continue` (não fatal, diferente de DamageFlags) | Fase2 (MultiSelectField) — fora do escopo desta rodada | — | fonte já em `SKILL_DB_FLAGS` (skill-db-yaml.ts), lista exata; **A6**: doc oficial chama um deles `IsShadowSpell`, constante real é `IsAutoShadowSpell` | médio hoje | Fase2b | feito |
| `castTimeFlags[]`/`castDelayFlags[]` | `TokenListField` CSV × 2 | `array(string()).default([])` × 2 | `"SKILL_CAST_"+s` bools | 3: `IgnoreDex IgnoreStatus IgnoreItemBonus` | skill.cpp:15235-15261; skill.hpp:163-167 | não | inválido → `return 0` | Fase2 (MultiSelectField) — fora do escopo desta rodada | — | fonte já em `SKILL_DB_CAST_FLAGS`; **conferir se cast-time e cast-delay usam a MESMA lista de 3 antes de ligar em Fase2** | alto hoje | Fase2b | feito |
| `hits`/`areaRadius`/`knockback`/`activeInstances`/`giveAp`/`spCost`/`castTimeMs.{variable,fixed}`/`cooldownMs`/`afterCastDelayMs`/`afterCastWalkDelayMs`/`durationMs`/`duration2Ms` | `PerLevelField` (texto) × 13 | `perLevel(int()).default(0)` cada | `parseNode(...)` várias sub-chaves | sem faixa fixa (livre); `SplashArea` sentinela -1=tela inteira/0=sem área/n=(2n+1)²; `FixedCastTime` sentinela -1=20% do CastTime | skill.cpp:15028-15226 (vários) | não | ver sentinelas | fora de escopo (per-level, não é `NumberField` simples) | — | nenhuma | baixo | fora de escopo | pendente |
| `unit`/`copyFlags`/`noNearNpc` | `JsonField` (JSON textarea) | objetos opcionais estruturados | `Unit.*`/`CopyFlags.*`/`NoNearNPC.*` | ver enums no C++ (`UNT_*` 185 valores, `SKILL_REQ_*` 15, `SKILL_NONEAR_*` 4, `BCT_*` 14, `UF_*` 18) | skill.cpp:15055-15153, 15583-15717 | condicional | `Unit.Id` obrigatório se `Unit` presente; `CopyFlags.Skill` obrigatório se `CopyFlags` presente | fora de escopo — Fase2 nota como opcional/avaliar depois, não confirmado nesta rodada | — | nenhuma | médio (JSON livre não valida nada hoje) | Fase2 (opcional) | não fazer nesta rodada |
| `appliedStatuses[].statusId` | Select de catálogo + escape "fora do catálogo" — **já é picker!** | `string()` | `"SC_"+s` (campo `Status`, só o [0] é gravado pelo writer) | catálogo | skill.cpp:15717 | não | — | sem mudança | — | **A7-adjacente**: writer só grava `appliedStatuses[0]`, resto vira warning silencioso | write-path lossy, já documentado no writer | fora de escopo | reportado |
| `appliedStatuses[].chance` | Input(number) | `number().min(0).max(100).default(100)` | não é campo direto do C++ — é parâmetro de script | livre | n/a (é `Status`, sem `Chance` no loader; chance é aplicada em runtime via script) | não | — | NumberField min=0 max=100 (já tem faixa no zod, só falta o controle) | min=0 max=100 step=1 | nenhuma | nenhum | Fase1 | feito |
| `appliedStatuses[].durationMs` | `PerLevelField` | `perLevel(int())` required | não é campo direto — script | livre | n/a | sim | — | fora de escopo (per-level) | — | nenhuma | baixo | fora de escopo | pendente |
| `appliedStatuses[].magnitude` | **sem UI** | `perLevel(number()).optional()` | não é campo direto — script | livre | n/a | não | — | fora do escopo (Fase1 não adiciona campo novo) | — | campo sem controle nenhum — achado | baixo | fora de escopo | reportado |
| `appliedStatuses[].appliesTo` | Select (target/self) | `z.enum(["target","self"]).default("target")` | não é campo direto — decisão de script | 2 valores | n/a | não | — | sem mudança (já fechado) | — | nenhuma | nenhum | — | feito |
| `spCost` | `PerLevelField` (topo do schema, não dentro de `requirements`) | `perLevel(int()).default(0)` | escrito em `Requires.SpCost` pelo writer (`skill-db-mapper.ts:210`) | livre | skill.cpp:15290-15352 (`Requires.SpCost`) | não | — | fora de escopo (per-level) | — | nenhuma | baixo | fora de escopo | pendente |

## Notas
- `hitType` (A7) e `flags`/`IsAutoShadowSpell` (A6) são achados de nomenclatura/write-path — não corrigidos nesta rodada, só registrados.
- `element`/`requiredEquipment`/`unit`/`copyFlags`/`noNearNpc` ficam de fora tanto da Fase 1 quanto da Fase 2 desta rodada — nenhum deles está na lista de 4 campos aprovados nem tem `NumberField` simples aplicável.
