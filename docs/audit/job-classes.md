# Matriz de auditoria — Classes (JobClass)

Fonte da verdade rAthena: `JobDatabase::parseBodyNode` (`rathena/src/map/pc.cpp:13819-14266`),
`db/re/job_stats.yml:18-83`. Schema: `packages/game-data/src/job-class.ts`.
UI: `apps/admin/components/JobClassForm.tsx`. Write path: `apps/api/src/store/job-database-writer.ts` +
`packages/game-data/src/rathena/job-class-mapper.ts` (2 arquivos: `job_stats.yml` + `skill_tree.yml`).

| Campo | UI hoje | Zod | rAthena — tipo | rAthena — faixa/enum | Fonte | Obrigatório? | Sentinelas | UI proposta | Restrição proposta | Divergência | Risco de compat | Fase | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | Input(number), disabled edit | `int().nonnegative()` (0 = Novice válido) | `"JOB_"+s` resolvido por nome, não id direto no loader — mas o projeto usa id numérico como chave | 194 `JOB_*` | script_constants.hpp:45-264 | sim | 0 = Novice | sem mudança (disabled) | — | nenhuma | nenhum | — | feito |
| `name` | Input(text) | `string().min(1)` | resolvido do id via tabela `JOB_*` do projeto, não campo livre no rAthena | n/a — nome vem do enum, não é digitável no servidor real | — | sim | — | sem mudança (fora do escopo — mudar isso reabriria decisão de schema) | — | campo do projeto | baixo | fora de escopo | pendente |
| `parentClassId` | Input(number) livre, `""`→null | `int().nullable().default(null)` | não existe campo direto — é DERIVADO na leitura (`job-class-mapper.ts:495`) a partir de `Inherit` do `skill_tree.yml` | — | job-class-mapper.ts:495 | não | — | Fase3 (picker de classe) — fora do escopo desta rodada | — | nenhuma | médio (id inexistente digitado é silenciosamente aceito hoje) | Fase3 | não fazer nesta rodada |
| `maxBaseLevel` | Input(number) | `int().positive()` (sem teto) | uint16 | **1..275** (`MAX_LEVEL`); `==0` → cap pra 275 (não falha) | pc.cpp:14029-14046; map.hpp:78 | sim | — | NumberField min=1 max=275 | min=1 max=275 step=1 int | **A7**: zod não tem teto | baixo (rAthena grampeia, não falha) | Fase1 | feito |
| `maxJobLevel` | Input(number) | `int().positive()` (sem teto) | uint16 | 1..275 | pc.cpp:14072-14089 | sim | mesma regra | NumberField min=1 max=275 | min=1 max=275 step=1 int | **A7** | baixo | Fase1 | feito |
| `maxWeight` | Input(number) | `int().positive()` (sem teto) | uint32 | sem checagem no parse; default real **20000** | pc.cpp:13855-13867 | sim | — | NumberField min=1 (sem teto — C++ não valida) | min=1 step=1 int | nenhuma (zod já sem teto, C++ também sem teto — batem) | nenhum | Fase1 | feito |
| `baseStats.{str,agi,vit,int,dex,luk}` | Input(number) × 6 | `int()` (BaseStatsSchema, sem min/max) | **não existe no `job_stats.yml`** — campo do projeto, sempre lido como 1 fixo no mapper (`job-class-mapper.ts:499`) | n/a | job-class-mapper.ts:499 (stub) | sim (schema) | valor real sempre 1, ignorado no write | sem mudança (não vale a pena limitar um campo que o writer descarta) | — | write-path: `baseStats` NUNCA é emitido pro job_stats.yml — edição é NO-OP contra o servidor | alto (usuário edita STR base achando que afeta o jogo, e não afeta) | fora de escopo (backend, já documentado no mapper) | reportado |
| `bonusStatsPerLevel[].level` | Input(number) | `int().positive()` | uint16 (`Level` de `BonusStats`) | 1..275 | pc.cpp:14115-14143 | sim | `==0 \|\| >275` → `return 0` (falha o job inteiro) | NumberField min=1 max=275 | min=1 max=275 step=1 int | **A7** | alto (fora da faixa derruba TODO o job no load) | Fase1 | feito |
| `bonusStatsPerLevel[].stats.{str..luk}` | Input(number) × 6, `""`→undefined | `BaseStatsSchema.partial()` | `int16` lido, armazenado em `array<uint16,PARAM_MAX>` — **wrap de negativo em positivo grande** | sem checagem de faixa no parse; representação real é `int16` (-32768..32767) | pc.cpp:14115-14143; pc.hpp:1093 | não | **A7-crítico**: valor negativo aqui faz WRAP para um uint16 gigante no servidor (bug do rAthena, não nosso) | NumberField min=-32768 max=32767, hint "negativo aqui gera valor absurdo no servidor (bug conhecido do rAthena — evite negativo)" | min=-32768 max=32767 step=1 int | **A7**: zod não tem limite, e mesmo dentro do limite um valor negativo tem efeito colateral grave no servidor real | **alto** — usuário grava bônus negativo pra "punir" uma classe, servidor faz overflow e o stat vira um número gigante | Fase1 (limite + hint forte) | feito |
| `bonusStatsPerLevel[].traits` | **read-only** (texto âmbar) | `z.record(string(),int()).optional()` | `BonusStats` chaves `Pow Sta Wis Spl Con Crt` (subconjunto de `PARAM_*`) | pc.hpp:203-232 | pc.cpp:14115-14143 | não | — | sem mudança (fora do escopo — não está nos campos numéricos simples, é um record dinâmico) | — | nenhuma | baixo | fora de escopo | pendente |
| `baseHpByLevel[]`/`baseSpByLevel[]` | `NumberListField` (textarea CSV) | `array(int()).default([])` × 2 | seq `{Level,Hp/Sp}`, só `#ifdef HP_SP_TABLES` | `Level` 0 ou >275 → `return 0` (falha o job) | pc.cpp:14143-14195 | não | `Level > maxBaseLevel` → silenciosamente ignorado (`continue`) | fora de escopo (é lista posicional por índice implícito, não um `NumberField` simples — mudar exigiria repensar a UI de tabela) | — | nenhuma | médio | fora de escopo | pendente |
| `baseExpByLevel[]`/`jobExpByLevel[]` | `NumberListField` (textarea CSV) | `array(number()).default([])` × 2 | seq `{Level,Exp(uint64)}` | mesma regra de Level acima; `MAX_LEVEL_BASE_EXP`=99999999, `MAX_LEVEL_JOB_EXP`=999999999 | pc.cpp:14046-14089; const.hpp:73-75 | não | — | fora de escopo (mesma razão) | — | nenhuma | médio | fora de escopo | pendente |
| `hpGrowth.{base,perLevel,factor}` / `spGrowth.{base,perLevel,factor}` | **read-only** (banner âmbar) | objeto opcional `{base,perLevel,factor}` números | `HpFactor`/`HpIncrease` (uint32, sem checagem) / `SpFactor`/`SpIncrease` | livre | pc.cpp:13867-13917 | não | default HpIncrease=500, SpIncrease=100 | sem mudança (já é read-only de propósito, fora do escopo desta rodada tocar) | — | nenhuma | baixo | fora de escopo | pendente |
| `skills[].skillId` | Input(number) livre | `int().positive()` | resolvido por nome aegis no writer, skill precisa existir | catálogo skill | job-class-mapper.ts (skill_tree) | sim (por linha) | id sem nome aegis conhecido → skip+warn no writer | Fase3 (picker de skill) — fora do escopo desta rodada | — | nenhuma | médio | Fase3 | não fazer nesta rodada |
| `skills[].maxLevel` | Input(number) | `int().nonnegative()` | `MaxLevel` no `Tree` do skill_tree.yml | **0..13** (0 = "remove skill herdada", é sentinela válido) | skill-tree-yaml.ts:32-44 | sim | 0 = remove herança | NumberField min=0 max=13 | min=0 max=13 step=1 int | nenhuma (zod já é nonnegative, só falta teto+hint do sentinela 0) | baixo | Fase1 | feito |
| `skills[].requires[]` | Input(text), mini-DSL `id:level` custom | `array({skillId,level}).default([])` | `Requires: [{Name,Level}]` | `Level` 0..13 | skill-tree-yaml.ts:32-44 | não | — | Fase3 (editor estruturado picker+NumberField, serializado de volta ao formato string) — fora do escopo desta rodada | — | nenhuma | médio (typo no mini-DSL é aceito sem erro visível) | Fase3 | não fazer nesta rodada |
| `skills[].baseLevel`/`.jobLevel` | **sem UI** | `int().min(0).max(275).optional()` × 2 | `BaseLevel`/`JobLevel` no `Tree` | 0..275 | skill-tree-yaml.ts:32-44 | não | — | fora do escopo (Fase1 não adiciona campo novo à UI) | — | campo sem controle nenhum — achado | baixo | fora de escopo | reportado |
| `skills[].exclude` | **sem UI** | `bool().optional()` | `Exclude` no `Tree` | — | skill-tree-yaml.ts:32-44 | não | — | fora do escopo | — | campo sem controle nenhum — achado | baixo | fora de escopo | reportado |
| `allowedWeapons[]`/`allowedArmorTags[]` | **sem UI** | `array(ItemSubTypeSchema).default([])` / `array(string()).default([])` | **não existem no rAthena job_stats.yml** — campos do projeto, sempre lidos como `[]` (`job-class-mapper.ts:508-509`) | n/a | job-class-mapper.ts:508-509 (stub) | não | — | fora do escopo (write-path é NO-OP, não vale adicionar UI pra campo que não persiste) | — | write-path: NUNCA emitido, sempre `[]` no read | alto (funcionalidade fantasma) | fora de escopo (backend) | reportado |
| `skillTreeInherit[]` | **sem UI** | `array(int().nonnegative()).default([])` | `Inherit: {parentName: true}` no skill_tree.yml | ids de job existentes | job-class-mapper.ts:388-405 (`jobClassToSkillTreeEntry`) | não | — | fora do escopo | — | **A3-adjacente**: `job-class-row.ts` (Supabase) não tem coluna pra este campo — reseta pra `[]` em round-trip Supabase, mesmo que MariaDB/YAML preserve | write-path lossy (só no backend Supabase) | fora de escopo (backend) | reportado |
| `aspdModifiers[].weaponType` | Select (37: `bare_hand`,`shield` + 35 `ItemSubTypeSchema`) | `ItemSubTypeSchema.or(enum(["bare_hand","shield"]))` | `"W_"+s` (`BaseASPD` chaves) | 25 reais: `Fist Dagger 1hSword 2hSword 1hSpear 2hSpear 1hAxe 2hAxe Mace 2hMace Staff Bow Knuckle Musical Whip Book Katar Revolver Rifle Gatling Shotgun Grenade Huuma 2hStaff Shield` | pc.cpp:13945-14001; job-stats-yaml.ts:42-46 | não | inválido → skip+warn | sem mudança na UI (já é Select), mas **A7**: schema oferece 37 opções (inclui ammo/card subtypes que não fazem sentido pra arma) vs 25 reais de `BaseASPD` | — | **A7**: zod (`ItemSubTypeSchema`, 35 valores incluindo munição/carta) é MAIS LARGO que o conjunto real de `JOB_ASPD_WEAPON_KEYS` (25) | médio — usuário pode selecionar "Arrow" como tipo de arma pra ASPD, o que não existe no rAthena | fora de escopo (mudar o Select exigiria estreitar pra 25, não está nos 4 campos aprovados) | reportado |
| `aspdModifiers[].baseAspd` | Input(number) | `z.number()` (não int!) | int16 | default `AMOTION_ZERO_ASPD`(2000)/`AMOTION_INTERVAL`(10) = **200 sob RENEWAL** | pc.cpp:13945-13968; status.hpp:61-67 | não | — | NumberField min=-32768 max=32767 (int16, faixa completa — mas conferir se schema realmente quer float aqui) | min=-32768 max=32767 step=1 | **A7**: zod é `number()` sem `.int()`, campo real do rAthena é `int16` (inteiro) — decimal aceito no zod nunca é válido no servidor | médio | Fase1 (min/max, sem forçar int — mudar `.int()` é edição de schema, fora de escopo) | feito |

## Notas
- `baseStats`, `allowedWeapons`, `allowedArmorTags` são os 3 achados mais graves desta entidade: campos totalmente editáveis na UI que **nunca chegam ao servidor real** — write-path é NO-OP silencioso, documentado no próprio código do mapper mas nunca superficializado pro usuário do admin.
