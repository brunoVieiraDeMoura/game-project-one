// game-project — override de conf/map_athena.conf
char_ip: 127.0.0.1
map_ip: 127.0.0.1

// Portas proprias — ver rathena-conf/login_conf.txt.
char_port: 6122
map_port: 5122

// Nossos NPCs. Ficam em npc-idle/ no repo e chegam aqui por symlink
// (npc/game-project), criado pelo scripts/wsl-setup.sh. Carregar por aqui
// evita editar qualquer arquivo dentro de rathena/.
npc: npc/game-project/devmenu.txt
npc: npc/game-project/panel.txt
// NPCs criados pelo admin (Fase 3.4, POST /npcs) — arquivo próprio, nunca
// mistura com script escrito à mão; ver npc-idle/admin-created.txt.
npc: npc/game-project/admin-created.txt

// Mapas customizados (Fase 3, testes funcionais reais). `map:` é o que faz
// ESTE map-server hospedar o mapa (map_addmap, map.cpp); sem esta linha o
// mapindex conhece o nome/id (rathena-db-import/map_index.txt) mas nenhum
// servidor responde por ele — o cliente trava em "conectando ao mapa".
map: gpqa01
npc: npc/game-project/mobs/gpqa01.txt
