# URL pública do gateway Socket.IO (apps/gateway). Vite grava no bundle no BUILD.
VITE_GATEWAY_URL=http://localhost:4100
# URL pública da API Fastify (apps/api). Idem.
VITE_API_URL=http://localhost:4000
